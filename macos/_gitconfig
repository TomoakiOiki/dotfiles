[user]
	name =  toiki
	email = tuiboku1128@gmail.com

[ghq]
	root = ~/src

[core]
	editor = vim
	excludesfile = /Users/tomoaki.oiki/.gitignore_global
[pull]
	default = current
	ff = only
[alias]
    p = !git push origin `git rev-parse --abbrev-ref HEAD`
    fp = !git push --force-with-lease origin `git rev-parse --abbrev-ref HEAD`

